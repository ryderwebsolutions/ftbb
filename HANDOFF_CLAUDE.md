# Handoff — Pause and next steps for Claude

Summary
- Pausing active work. This document lists recent changes, important notes, and the next actions for Claude (or another engineer) to pick up.

What I changed (high level)
- Created partner landing page: partners/graham-shiels-mortgages.html (two versions currently present; needs cleanup).
- Added `css/shared-header-footer.css` and started using it across pages.
- Replaced header/footer on several pages with the standardized `site-header` and `site-footer` components while preserving JS hooks.
- Updated `partners.html` and `index.html` to link to the Graham landing page.

Files edited (not exhaustive)
- css/shared-header-footer.css — shared header/footer styles.
- partners/graham-shiels-mortgages.html — landing page (duplicate content; keep the standardized version and remove the earlier duplicate).
- partners.html — partner grid + Graham card linking to the partner page.
- index.html — header updated; partner carousel updated to include Graham link.
- advertise.html, buy.html, cookies.html, landing.html, media-kit.html, privacy-policy.html, gilmartin-exclusive.html — header/footer replaced or standardized.

Important notes / gotchas
- Preserve the following JS hooks in the header: `nav` (element id), `nav-ham` (hamburger button id) and `nav-mobile` (mobile menu id). Inline scripts rely on these ids (scroll class toggle, mobile open/close).
- `partners/graham-shiels-mortgages.html` contains two full HTML documents (an older original and the new standardized version). Remove the duplicate older document and keep the standardized one that uses `/css/shared-header-footer.css` and `site-header` / `site-footer`.
- Some pages had the old `<footer class="footer">` removed and replaced by `<footer class="site-footer">`. Verify that links and content (addresses, contact emails) are preserved if needed.
- CSS include path used across pages: `/css/shared-header-footer.css` — prefer absolute path to avoid relative path bugs when pages are nested.
- Logos and placeholder images are in the `logos/` directory. Confirm filenames when bulk-generating pages.

Remaining tasks for Claude
1. Clean duplicates and normalize `partners/graham-shiels-mortgages.html` (remove the first duplicated HTML block).
2. Sweep the repo for any remaining `footer` / legacy nav instances and replace with the `site-header` / `site-footer` pattern (ensure `id="nav"`, `id="nav-ham"`, and `id="nav-mobile"` are present where needed).
3. Verify `index.html` carousel duplication pattern — it has two tracks; update both if adding new partner logos.
4. Run a crawler for remaining partner URLs (~33) to extract Instagram / Facebook / LinkedIn links and map them to partner slugs.
5. Bulk-generate `partners/<slug>.html` pages using the standardized Graham template. For each generated page:
   - Use the logo from `logos/` when available.
   - Insert social links with `target="_blank" rel="noopener noreferrer"`.
   - Ensure header includes `id="nav"` and the mobile nav ids.
6. Test pages locally (see commands) and verify that the Instagram CTA opens in a new tab and that the mobile menu continues to function.

Quick commands
- Find remaining legacy footers and navs (PowerShell):
```powershell
Get-ChildItem -Recurse -Include *.html | Select-String -Pattern '<footer class="footer"|id="nav"|nav-ham|nav-mobile' -List | Select-Object Filename, LineNumber, Line
```
- Open a minimal static server (Node.js `http-server` or `serve`) from repo root to check pages in browser:
```powershell
# if you have npm install
npx http-server . -p 8080
# or
npx serve -p 8080
```

Testing checklist
- On a running local server, visit `/partners/graham-shiels-mortgages.html` and verify:
  - Page loads and shows a single standardized header/footer (no duplicate HTML)
  - Instagram CTA opens in a new tab
  - Mobile nav open/close works (click hamburger)
- Visit `privacy-policy.html` and ensure the cookie bar and inline footer scripts (nav scroll, mobile nav) still run correctly.

Assets & locations
- Logos: `logos/`
- Shared header/footer CSS: `css/shared-header-footer.css`
- Partner pages: `partners/`

Notes for automation (bulk generation)
- Use the Graham template file (the standardized HTML block toward the bottom of `partners/graham-shiels-mortgages.html`) as the template.
- Required variables per partner: `name`, `slug`, `logo_filename` (if exists), `short_description`, `socials` (instagram, facebook, linkedin), optional images.
- Prefer creating files with consistent slugs (kebab-case) matching `partners/<slug>.html`.

Contact
- If anything is unclear, check the recent edits and history in this repository branch `main`.

----
Generated by the previous agent to pause work and hand off to Claude. Please confirm when you start and update the status in the todo list.
